# FinCalc-DSA

A C++ learning project to buld a **financial calculation system** using **core DSA concepts** (arrays,linked lists, search/sort, FIFO/LIFO scheduling).

## Build & Run
```bash
make run
# or:
make && ./bin/fincalc
```