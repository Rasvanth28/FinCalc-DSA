
**Q: Why C++ over rest of the programming languages**

**A:** C++ offers the perfect balance between low-level control and high-level abstraction—ideal for a DSA-based system—allowing both manual implementation of data structures and use of STL for efficiency, combining academic depth with professional-grade performance.

**Q: Why we are not using**
```bash
 using namespace std
```
**A:** 

**Q: Why we are using**
``` bash
std::ios::sync_with_stdio(false) and std::cin.tie(nullptr)
```

**A:**

**Q: Why we are using #pragam once**

**A:**


**Q: What does these two lines does**
 ```bash 
std::cin.clear() 
std::cin.ignore(std::numeric_limits<std::streamsize>::max(),\n)
```
**A:**

**Q: Why are we using memory library**
```bash
#include <memory>
```

**A:**





